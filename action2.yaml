name: 'Barsali_Ciao_mondo'
description: 'Saluta qualcuno'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
runs:
  using: "composite"
  steps:
    - run: echo ciao ${{ inputs.who-to-greet }}.
      shell: bash